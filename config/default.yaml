data:
  root: /home/a123456/SEA-/MMFi
  seq_len: 20   #  从10恢复到20（提升时序建模）

train:
  batch_size: 8   #  核心提升（显存主力）
  epochs: 50
  lr: 0.0001

model:
  dim: 128   #  从64→128（模型能力翻倍）
  num_joints: 17

domain:
  source: [E01, E02, E03]
  target: [E04]

log:
  save_dir: ./outputs
  print_freq: 20